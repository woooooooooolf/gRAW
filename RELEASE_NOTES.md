# gRAW v0.0.9 发布说明

gRAW 是一款面向 Windows 的便携式 Bayer RAW 测试图像生成器，用于传感器图像链路开发、解码器验证、图像质量实验和自动化测试数据构造。这是 gRAW 的首个正式标签发布，包含 0.0.1 至 0.0.9 的全部迭代成果。

## 核心功能

- **图像生成**：支持 Mono、四种标准 2×2 Bayer 阵列和四种 Quad Bayer 阵列；生成确定性的无文件头 RAW 图像。
- **存储格式**：Unpacked 8-bit / 16-bit、MIPI RAW10 / RAW12 / RAW14，支持 8–16 bit 采样位深，Unpacked 16-bit 支持大小端与 LSB/MSB 对齐。
- **布局控制**：行对齐、帧对齐、文件起始偏移、区域填充字节、多帧输出。
- **测试图案**：黑白与彩色图案分组（彩条、彩色渐变、RGB 渐变、灰阶渐变、棋盘格、随机噪声等），并根据 CFA 自动过滤不兼容选项。
- **桌面界面**：单页工作台，中英文 UI、六套配色主题、三档阅读字号。
- **生成体验**：精确布局预览、预计文件大小、参数校验、生成进度与取消操作。
- **合规**：MIT OR Apache-2.0 双许可证，应用内提供第三方开源组件清单。

## 本版变更亮点

- 新增彩色测试图案与分组菜单（v0.0.7）。
- 阅读字号与窗口缩放分离，优化大字号下生成信息的显示（v0.0.5 / v0.0.6）。
- 优化图案兼容逻辑与界面信息层级（v0.0.5）。
- 工作台等比缩放与六套配色主题（v0.0.2 / v0.0.3）。
- 中英文 README、CI 质量检查与自动发布流程（本版）。

## 成果物与校验

本发布提供以下成果物：

- `gRAW-windows-x64-v0.0.9.exe`：便携式可执行文件，无需安装。
- `gRAW-windows-x64-v0.0.9.exe.sha256`：SHA-256 校验文件。

校验方法（PowerShell）：

```powershell
Get-FileHash -Algorithm SHA256 .\gRAW-windows-x64-v0.0.9.exe
```

将输出中的哈希值与 `.sha256` 文件中的内容比对即可确认文件完整性。
